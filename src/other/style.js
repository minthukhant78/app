import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    viewPrincipal: {
        backgroundColor: '#fff',
        borderRadius: 20,
        padding: 20,
        margin: 10,
    },
});

export default styles