import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    viewContact: {
        flex: 1,
        backgroundColor: '#fff',
        borderRadius: 20,
        padding: 20,
        margin: 10,
    },
    textInformation: {
        color: '#26A69A',
        marginTop: 5
    }
});

export default styles